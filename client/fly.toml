# fly.toml app configuration file generated for hd-assig on 2025-10-31T18:15:00+05:30
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'hd-assig'
primary_region = 'bom'

[build]
  [build.args]
    NEXT_PUBLIC_API = "https://server-broken-hill-5642.fly.dev"

[env]
  NEXT_PUBLIC_API = "https://server-broken-hill-5642.fly.dev"

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
